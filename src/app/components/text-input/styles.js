import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
    textField: {
        width: '100%',
        marginTop: 10,
        marginBottom: 10,
    },
}))
